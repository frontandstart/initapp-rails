<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type">
    <title>Rubyvkube - Разработка комплексных решений</title>
    <meta content="width=device-width,initial-scale=1" name="viewport">

    <% meta_content = meta_tags(@meta_tags || {}) %>
    <title><%= meta_content[:title] %></title>
    <meta name="description" content="<%= meta_content[:description] %>">
    <meta name="keywords" content="<%= meta_content[:keywords] %>">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:title" content="<%= meta_content[:og][:title] %>">
    <meta property="og:description" content="<%= meta_content[:og][:description] %>">
    <meta property="og:image" content="<%= meta_content[:og][:image] %>">
    <meta property="og:url" content="<%= meta_content[:og][:url] %>">
    <meta property="og:type" content="<%= meta_content[:og][:type] %>">
    <meta property="og:locale" content="ru_RU">
    <meta property="og:site_name" content="Rubyvkube">
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:title" content="<%= meta_content[:title] %>">
    <meta property="twitter:description" content="<%= meta_content[:description] %>">
    <meta property="twitter:image" content="<%= meta_content[:og][:image] %>">
    
    <!-- Additional SEO -->
    <meta name="robots" content="index, follow">
    <meta name="language" content="ru">
    <meta name="revisit-after" content="7 days">
    <meta name="author" content="Rubyvkube">
    <meta name="geo.region" content="RU">
    <meta name="geo.placename" content="Россия">
    <meta name="theme-color" content="#1e293b">
    <link rel="canonical" href="<%= request.original_url %>">
    
    <!-- Yandex verification (добавьте код после регистрации в Яндекс.Вебмастер) -->
    <!-- <meta name="yandex-verification" content="YOUR_YANDEX_VERIFICATION_CODE"> -->
    
    <!-- Google verification (добавьте код после регистрации в Google Search Console) -->
    <!-- <meta name="google-site-verification" content="YOUR_GOOGLE_VERIFICATION_CODE"> -->
    
    <!-- Schema.org structured data -->
    <script type="application/ld+json" data-turbo-suppress-warning>
    {
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "Rubyvkube",
      "url": "https://rubyvkube.ru",
      "logo": "<%= request.base_url %>/icon.png",
      "contactPoint": {
        "@type": "ContactPoint",
        "email": "contact@rubyvkube.ru",
        "telephone": "+7-999-123-4567",
        "contactType": "customer service"
      },
      "address": {
        "@type": "PostalAddress",
        "addressCountry": "RU"
      },
      "sameAs": [],
      "description": "Профессиональная разработка веб-приложений, мобильных приложений, бэкенд-систем и DevOps-решений"
    }
    </script>
    
    <% if @structured_data %>
      <!-- Page-specific structured data -->
      <script type="application/ld+json" data-turbo-suppress-warning>
      <%= raw @structured_data.to_json %>
      </script>
    <% end %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= yield(:javascript) %>
  </head>
  <body>
    <%= yield %>
    <%= render 'shared/footer' %>
  </body>
</html>